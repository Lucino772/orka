[project]
name = "orka-api"
version = "0.1.0"
description = "orka-api"
requires-python = ">=3.12.12"
dependencies = [
    "hypercorn>=0.18.0",
    "starlette>=1.0.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
