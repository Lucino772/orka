[project]
name = "orka-db"
version = "0.1.0"
description = "orka-db"
requires-python = ">=3.12.12"
dependencies = [
    "alembic[tz]>=1.18.4",
    "psycopg[binary]>=3.3.3",
    "sqlalchemy>=2.0.49",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
